
# MongoDB Configuration (NO pongas credenciales reales aquí)
MONGO_ROOT_USERNAME=<tu_usuario>
MONGO_ROOT_PASSWORD=<tu_password>

# Copia este archivo a .env y pon tus credenciales reales SOLO en .env
# NUNCA subas .env ni credenciales reales al repositorio
# .env está en .gitignore y no será versionado
